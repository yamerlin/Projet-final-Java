CREATE TABLE users
(
    id INT PRIMARY KEY NOT NULL,
    nom VARCHAR(100),
)


CREATE TABLE scores
(
    id INT PRIMARY KEY NOT NULL,
    victoireParExecution int,
	victoireLoyal int,
	defaiteParExecution int,
	defaiteLoyal int,
	tentativeExectuion int,
	jokerUtilise int
)

ALTER TABLE scores
ADD userId int;

ALTER TABLE scores
ADD FOREIGN KEY (userId) REFERENCES users(id);